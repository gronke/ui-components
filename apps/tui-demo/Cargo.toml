[package]
name = "uic_tui_demo"
description = "Terminal demo: ui-components rendered by uic_tui"
publish = false
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
# The catalog inputs' terminal twins live in the companion crate (ADR 0002);
# it chains ui_components for the definitions.
ui_components_tui.workspace = true
# The demo composition <app-root> mounts by tag beside the catalog inputs.
ui_components_demo.workspace = true
uic_core.workspace = true
uic_tui.workspace = true
