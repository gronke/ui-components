// The app component comes from the generated root and imports its child
// components itself; the imports resolve in the merged output tree
// (`web/` + generated components side by side).
import './components/app-root.js';
