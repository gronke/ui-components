[package]
name = "ui_components_demo"
description = "The catalog's demo composition: <app-root> assembling the inputs around one state object"
publish = false
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
uic_core.workspace = true
# The connector abstractions the word pool rides (ADR 0014) live in the catalog.
ui_components.workspace = true
