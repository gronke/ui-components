[package]
name = "uic_sync"
description = "State sync tooling for lit components: structured-clone snapshots over one wire seam, plus serverless QR pairing"
version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
# Compiles the TypeScript sources into the publish-ready npm tree.
uic_npm.workspace = true
# Builds the tree's exports and description into the TreeSpec.
serde_json.workspace = true
# The compact payload codec in Rust, byte-compatible with web/pair.ts.
serde.workspace = true
base64.workspace = true
